React allows us to conditionally render components which means that the developer can decide 
which component to render on the screen on on the basis of some predefined conditions. We can do this using if else statements, logical operators, ternary operators etc. 
